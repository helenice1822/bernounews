import {useState} from 'react';import Layout from '../components/Layout';export default ()=>{const [p,setP]=useState('');async function enter(e){e.preventDefault();const r=await fetch('/api/news/admin-check',{headers:{'x-admin-password':p}});if(r.ok){localStorage.setItem('bn_pwd',p);alert('Admin ativo!');}else alert('Senha errada');}return <Layout><h2>Admin</h2><form onSubmit={enter}><input type='password' placeholder='Senha' value={p} onChange={e=>setP(e.target.value)}/><button>Entrar</button></form></Layout>};